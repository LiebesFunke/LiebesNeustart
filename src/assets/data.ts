import { getLocale } from './locales';

const locale = getLocale();

export const profiles = locale.profiles;
export const howItWorksSteps = locale.howItWorksSteps;
export const features = locale.features;
export const quizQuestions = locale.quizQuestions;
export const footerLinks = locale.footerLinks;
export const ui = locale.ui;
